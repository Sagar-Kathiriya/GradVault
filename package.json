{
  "name": "gradvault",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.16.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  }
}
