{
  "name": "gradvault",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "register:issuer": "REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000 hardhat run scripts/register-issuer.js",
    "write-env": "node scripts/write-env.js"
  },
  "devDependencies": {
    "hardhat": "^2.16.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  }
}
