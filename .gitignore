node_modules/
dist/
artifacts/
cache/
.env
.env.*
/idea
.vscode
# Allow lock file for reproducible CI installs
# (previously ignored, needed by GitHub Actions cache)
# Remove the following line to keep lock file tracked
# package-lock.json
